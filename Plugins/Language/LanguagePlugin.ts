import { DefinePlugin, Plugin } from "../../DefinePlugin";

export = DefinePlugin({
  name: "Language",
  description: "Change the language of Zygote!",
  commands: [],
  public_plugin: true
}) as Plugin;
